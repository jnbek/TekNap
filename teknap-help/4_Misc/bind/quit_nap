Synopsis:
   bind <key> quit_nap

Description:
   This function causes the client to sign off from the nap network.

Aliases:
   Thus function has the same effect as the QUIT command, except that a
   specific QUIT reason cannot be specified.

See Also:
   quit(1)

