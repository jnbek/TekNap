Synopsis:
   on [<modes>]browse_end [<serial#>] [-|^]<match> { <action> }

Description:
   This hook is triggered when the browse the user attempted finishes

Parameters:
   $0 are the search params
   $1 is the number of results found

Examples:

See Also:
   on(5) browse_begin

