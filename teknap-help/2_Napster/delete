Synopsis:
    delete <#|*>

Description:
    If there was a problem with a transfer then you might need to delete the
    file from the list of transfers. # can be a single number or a range of
    numbers. 

Examples:
    To delete a transfer:
         /delete 3
    To delete various transfers:
         /delete 3,5,6-9
    To delete all:
         /delete *
